///////////////// TEST ///////////////////

module halfadder_structural (
    input a,
    input b,
    output sum,
    output carry
);

  xor x1 (sum, a, b);
  and a1 (carry, a, b);

endmodule




//////////////// TEST BENCH CODE ////////////////

`timescale 1ns/1ps
module halfadder_structural_tb;
  
  reg a;
  reg b;
  wire sum;
  wire carry;
  
  halfadder_structural HAS (
                .a(a),
                .b(b),
                .sum(sum),
                .carry(carry)
         );
  
  initial begin 
    $dumpfile("halfadder_structural.vcd");
    $dumpvars(0,halfadder_structural_tb);
    
    a=0; b=0; #10;
    a=1; b=0; #10;
    a=0; b=1; #10;
    a=1; a=1; #10;
    
  end
endmodule
    